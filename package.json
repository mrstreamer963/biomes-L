{
  "name": "biomes",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "for port in 5173 5174 5175; do lsof -ti:$port | xargs kill -9 2>/dev/null; done && bash scripts/build-wasm.sh --dev && vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "pixi.js": "^8.0.0"
  },
  "devDependencies": {
    "vite": "^8.1.3",
    "@vitejs/plugin-vue": "^6.0.7",
    "typescript": "^6.0.3",
    "vue-tsc": "^3.3.6"
  }
}